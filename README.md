# module-user_filter_prefs
Xataface module that persists and restores per-user list filters (session or DB), with no redirects, mobile filter dialog support, and explicit exclusion of related-list contexts
